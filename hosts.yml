all:
  children:
    dev:
      hosts:
        "localhost:49100":
          ansible_user: pi
          ansible_become: yes
          ansible_become_method: sudo
    prod:
      hosts:
        "192.168.0.2":
          ansible_user: pi
          ansible_become: yes
          ansible_become_method: sudo
