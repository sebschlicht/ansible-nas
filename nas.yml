---
- name: Install Samba, required utils, miniDLNA and ownCloud
  hosts: dev
  become: yes
  user: pi
  vars:
    ansible_python_interpreter: /usr/bin/python3
  roles:
    - nas
    - samba
    - minidlna
